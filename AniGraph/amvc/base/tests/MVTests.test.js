import AModel from "../mvc/AModel";
import AView from "../mvc/AView";


test('Test AModelListener', () => {
    // const model = new AModel({name: 'TestModel'});
    // const view = new AView({name: 'TestView', model: model});
    expect(1).toBe(1);
});